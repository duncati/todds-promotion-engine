package org.duncati.promotionengine;

/**
 * Lists the different types of repositories. At the time of this writing there are 2, in memory and database which
 * are better described in RepositoryFactory.
 */
public enum RepositoryType {
     IN_MEMORY, DATABASE
}
